# GUI Requirements for COMPASS
# Install with: pip install -r requirements-gui.txt

# PyQt6 for GUI framework
PyQt6>=6.4.0

# pyqtgraph for fast plotting and image display
pyqtgraph>=0.13.0

# NumPy (likely already installed with COMPASS)
numpy>=1.20.0

# Matplotlib for colormaps
matplotlib>=3.5.0

# docopt for command-line argument parsing (likely already installed)
docopt>=0.6.2

# qtconsole for IPython console widget
qtconsole>=5.4.0
ipykernel>=6.20.0

# pyzmq for remote supervisor communication
pyzmq>=25.0.0
