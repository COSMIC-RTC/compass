# Minimal requirements for COMPASS Remote Client
# This is for client machines that don't need full COMPASS installation
# No CUDA, no sutra/carma required

# Core GUI dependencies (required)
PyQt6>=6.4.0
pyqtgraph>=0.13.0
numpy>=1.20.0
pyzmq>=25.0.0

# Optional but recommended
matplotlib>=3.5.0  # For better colormaps
qtconsole>=5.4.0   # For IPython console
ipykernel>=6.20.0  # For IPython console backend

# Note: These are sufficient for the remote client
# The server side needs full COMPASS installation
